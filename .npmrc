# pnpm: be patient on slower networks (large native binaries like @next/swc)
fetch-timeout=600000
fetch-retries=8
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=120000
# slow link: one download at a time so large native binaries get full bandwidth
network-concurrency=1
